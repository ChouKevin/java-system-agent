# test-repo Summary

`test-repo` is a representative example for using this repository. It contains a small order workflow, source files under `src/main/java`, mapper resources under `src/main/resources`, and business documents under `docs`.

The expected usage path is:

1. Read `repos/service-map.md` to discover `test-repo`.
2. Read `repos/test-repo/docs/business-map.md` to choose a business group.
3. Read `repos/test-repo/docs/business-groups/{groupName}.md` for entry points and required input data.
4. Run source analysis on the listed entry point when detailed flow evidence is needed.
