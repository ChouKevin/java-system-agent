package com.java.semantic.semantic.adapter.jdtls;

/** JDT workspace activities that keep a session ineligible for eviction. */
public enum WorkspaceActivityKind {
    REQUEST,
    DOCUMENT,
    INDEXING
}
