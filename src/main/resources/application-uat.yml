spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:}
    google:
      genai:
        api-key: ${GOOGLE_GENAI_API_KEY:}
        chat:
          options:
            model: ${GOOGLE_GENAI_MODEL:gemini-3.1-flash-lite}
            # thinking-level: HIGH
