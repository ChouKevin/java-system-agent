Use type-member discovery to inspect members of a typed owning type. Provide the requested member kinds and an optional name prefix when they help resolve the question. Keep result limits and offsets within the schema bounds. After an empty result, change a valid filter or resolve the remaining uncertainty.
