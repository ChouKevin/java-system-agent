Use type-member discovery after the owning type is known. For an initial request, select the member kinds and optional name prefix needed to resolve the question. For a provider continuation with a nonzero offset, retain its member filter and tune only the bounded result limit. A method member can unlock implementation, source, and call-graph follow-ups; a field member can unlock internal-reference follow-ups.
