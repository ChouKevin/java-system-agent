Use type-member discovery after the owning type is known. For an initial request, select METHOD, FIELD, ENUM_CONSTANT, and/or RECORD_COMPONENT plus any optional name prefix needed to resolve the question. For a provider continuation with a nonzero offset, retain its member filter and tune only the bounded result limit. METHOD can unlock implementation, source, and call-graph follow-ups; FIELD, ENUM_CONSTANT, and RECORD_COMPONENT can unlock internal-reference follow-ups. A complete empty result means this type has no members matching the requested kinds and prefix.
