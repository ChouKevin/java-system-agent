spring:
  profiles:
    active: dev
  application:
    name: java-system-agent
  http:
    client:
      connect-timeout: 10s
      read-timeout: 180s

logging:
  level:
    '[com.java.system.agent]': INFO
