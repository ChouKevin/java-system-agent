# test-repo Summary

`test-repo` is a representative example for using this repository. It contains a small order workflow, source files under `src/main/java`, mapper resources under `src/main/resources`, and business documents under `knowledge/repos/test-repo`.

The expected usage path is:

1. Read `knowledge/service-map.md` to discover `test-repo`.
2. Read `knowledge/repos/test-repo/business-map.md` to choose a business group.
3. Read `knowledge/repos/test-repo/business-groups/{groupName}.md` for entry points and required input data.
4. Run source analysis on the listed entry point when detailed flow evidence is needed.
